<!DOCTYPE html>
<html>
<head>
  <title>Register - Bank Nkhonde</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application" %>
  <%= stylesheet_link_tag "register" %> <!-- New animation CSS -->
  <%= javascript_include_tag "application" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <div class="register-container">
    <div class="register-card">
      <h2>Register</h2>

      <%= form_with url: '/auth/register', method: :post, local: true do |f| %>
        <div>
          <label>Phone Number:</label>
          <div class="phone-input-wrapper">
            <span class="country-code">+265</span>
            <%= f.telephone_field :phone, placeholder: "99xxxxxxx", class: "phone-input" %>
          </div>
        </div>

        <div>
          <label>Username:</label>
          <%= f.text_field :username %>
        </div>

        <div>
          <label>Password:</label>
          <%= f.password_field :password %>
        </div>

        <div>
          <label>Confirm Password:</label>
          <%= f.password_field :password_confirmation %>
        </div>

        <div>
          <%= f.submit "Register" %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <p style="color: red;"><%= flash[:alert] %></p>
      <% end %>
    </div>
  </div>
</body>
</html>